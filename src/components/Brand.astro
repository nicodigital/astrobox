---
const baseUrl = import.meta.env.BASE_URL;
const { width } = Astro.props;

---

<a href={baseUrl} class={`${width} block`} >
    <img src={`/img/logos/logo.svg`} alt="" loading='lazy' decoding='async' />
</a>