---
const { title , description, img } = Astro.props;
import Picture from '../components/Picture.astro'
// console.log(baseUrl)

---
<a href={img} class="card bg-neutral-800 text-white px-2 py-3 flex flex-col gap-2 rounded-xl">
    
    <Picture url={img} />

    <h2 class="h2 font-bold" >
        {title}
    </h2>

    <p>
        {description}...
    </p>

</a>